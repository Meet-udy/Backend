spring:
  application:
    name: meetudy

  datasource:
    hikari:
      maximum-pool-size: 4
    url: jdbc:mysql://localhost:3306/meetudy
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: true